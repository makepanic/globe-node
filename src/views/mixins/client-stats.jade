mixin clientStats(graphUrl, period, avg)
    .pure-g-r.graph-stats
        .pure-u-1-3
            h3 Clients
            p average connected clients
            strong
                span.d3-col.block-0
                = avg.averageClients
        .pure-u-2-3
            img(src=graphUrl + '?period=' + period,alt='Relay bandwidth graph')