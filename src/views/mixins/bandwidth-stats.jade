mixin bandwidthStats(graphUrl, period, avg)
    .pure-g-r.graph-stats
        .pure-u-1-3
            h3 Bandwidth
            p mean written bytes
            strong
                span.d3-col.block-0
                = format.bandwidth(avg.readHistory)
            p mean read bytes
            strong
                span.d3-col.block-1
                = format.bandwidth(avg.writeHistory)
        .pure-u-2-3
            .graph-holder
                img(src=graphUrl + '?period=' + period,alt='Relay bandwidth graph')